pipeline {
    agent any 
         stages {
             stage( "gt check out "){
                 steps{
                     script{git branch: 'main', url: 'https://github.com/lavanyaprashola/first-level.git'}
                 }
               }
             }
         }
